Выполнил студент группы 609-21
Худолеев Михаил Сергеевич
## Цель
Исследовать возможности стандартной архитектуры рекуррентной нейронной сети для решения задач прогнозирования временного ряда.
## Задание

| № варианта                          | 2                           |
| ----------------------------------- | --------------------------- |
| Объект                              | Имбирь<br>Ginger            |
| Целевая переменная                  | Минимальная цена<br>Minimum |
| Размер скрытого состояния           | 64                          |
| Длина последовательности (контекст) | 10                          |
## Ход работы
Работа выполнена в среде Google Colab.

![[Pasted image 20260319101339.png]]
*Импорт данных и Pandas-преобразование*

![[Pasted image 20260319101431.png]]
*Нормализация данных и подготовка обучающей и тестовой выборок*

![[Pasted image 20260319101518.png]]
*Подготовка последовательностей*

![[Pasted image 20260319101550.png]]
*Создание класса модели*

![[Pasted image 20260319101740.png]]
*Подготовка DataLoader'ов*

![[Pasted image 20260319101834.png]]
*Обучение модели*

![[Pasted image 20260319101911.png]]
*График ошибки во время обучения*

``` python
# Генерация предсказаний на тестовой выборке
model.eval()
predictions = []
with torch.no_grad():
    for inputs, _ in test_loader:
        inputs = inputs.cuda()
        outputs = model(inputs.float())
        predictions.extend(outputs.cpu().numpy())

predictions = np.array(predictions).reshape(-1, 1)
y_test_np = y_test.numpy().reshape(-1, 1)

# Обратная нормализация предсказаний и фактических значений
pred_original = scaler.inverse_transform(predictions)
y_test_original = scaler.inverse_transform(y_test_np)

# Восстановление полного ряда фактических данных для визуализации
data_original = scaler.inverse_transform(normalized_data).flatten()
test_start_idx = train_size + seq_length  # индекс начала тестовой выборки в исходном ряду

# Построение графиков
plt.figure(figsize=(14, 6))
# Фактические данные на всём диапазоне
plt.plot(data_original, label='Фактические данные', linewidth=1, alpha=0.7)

# Предсказания модели на тестовой выборке
plt.plot(range(test_start_idx, test_start_idx + len(pred_original)),
         pred_original, label='Предсказания модели', linewidth=2, color='red')
plt.xlabel('Временной шаг', fontsize=12)
plt.ylabel('Максимальная цена (импортированные единицы)', fontsize=12)
plt.title('Фактические данные и предсказания RNN-модели', fontsize=14)
plt.legend(fontsize=10)
plt.grid(True, alpha=0.3)
plt.tight_layout()
plt.show()
```
*Код генерации графика предсказаний*

![[Pasted image 20260319102116.png]]
*График предсказаний*

![[Pasted image 20260319102137.png]]
*Расчёт метрик качества прогноза*
## Вывод
В ходе работы были приобретены навыки создания и обучения рекуррентных нейронных сетей а также подготовки метрик точности работы модели.